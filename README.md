# BACKENDPROJECT
first in my project it have a node_modules but unfortunately it to big so i cant upload it to github But it just a libraries and packages as we kown
it a backend project that i make to study about backend how it can connet to our website how it work how to make it 
# TECHNOLOGY
in a libraries and packages i use
Database: SQLite (Used to store user data like credentials).

Authentication/Security: The implementation uses concepts and libraries related to:

Middleware (For running functions before route handlers, essential for checking authentication status).

Cookies (For managing user sessions).

Json Web Tokens (JWT) (A common package-based approach for securely transmitting user identity).

and coding i use
Programming Language     JavaScript
Runtime Environment      Node.js
Web Framework            Express.js
Templating Engine        EJS (Embedded JavaScript) for rendering login/signup pages
